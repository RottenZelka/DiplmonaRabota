{
    "name": "my-app",
    "version": "1.0.0",
    "scripts": {
      "build": "cd react-frontend && npm install && npm run build",
      "postinstall": "cd yii2-backend && composer install"
    },
    "engines": {
      "node": "14.x",
      "php": "7.4.x"
    }
  }